# NewPython-Host 项目逻辑架构

---

## 一、整体架构图

```mermaid
graph TB
    subgraph Client["客户端"]
        FE["前端应用 / 第三方调用"]
    end

    subgraph App["FastAPI 应用 (main.py)"]
        direction TB
        MW["中间件层"]
        
        subgraph Routers["路由层 (routers/)"]
            direction LR
            subgraph CA["CommercialApi<br/>/CommercialApi/*"]
                CA_REV["revenue_router<br/>营收管理 (50+接口)"]
                CA_BIG["bigdata_router<br/>大数据分析"]
                CA_BASE["base_info_router<br/>基础信息"]
                CA_EXAM["examine_router<br/>考核管理"]
                CA_CTR["contract_router<br/>合同分析"]
                CA_BUD["budget_router<br/>财务预算"]
                CA_CUS["customer_router<br/>客群分析"]
                CA_ANA["analysis_router<br/>分析说明"]
                CA_ABN["abnormal_audit_router<br/>异常稽查"]
                CA_BP["business_process_router<br/>业务审批"]
            end
            
            subgraph EA["EShangApiMain<br/>/EShangApiMain/*"]
                EA_BI["base_info/ (22个路由)<br/>基础信息管理"]
                EA_CT["contract/ (9个路由)<br/>合同/经营项目"]
                EA_FN["finance/ (4个路由)<br/>财务/票据/预算"]
                EA_RV["revenue/ (1个路由)<br/>营收数据"]
                EA_BD["bigdata/ (1个路由)<br/>大数据车流"]
                EA_MC["merchants/ (1个路由)<br/>商户管理"]
                EA_BT["batch_modules/ (6个路由)<br/>审核/支付/分析等"]
                EA_VD["video/ (1个路由)<br/>视频监控"]
            end
        end

        subgraph Shared["公用层"]
            direction LR
            CORE["core/<br/>核心工具"]
            MODELS["models/<br/>数据模型"]
            DEPS["routers/deps.py<br/>依赖注入"]
            SVC["services/<br/>业务逻辑层"]
        end
    end

    subgraph DB["数据存储"]
        DM["达梦数据库<br/>192.168.1.99:5236"]
        REDIS["Redis<br/>127.0.0.1:6379"]
        OLD["C# 旧接口<br/>192.168.1.99:8900"]
    end

    FE --> MW
    MW --> CA
    MW --> EA
    CA --> CORE
    CA --> MODELS
    CA --> DEPS
    EA --> CORE
    EA --> MODELS
    EA --> DEPS
    EA --> SVC
    SVC --> CORE
    CORE --> DM
    CORE --> REDIS
    CORE --> OLD
```

---

## 二、两个模块的关系

### 2.1 是否有穿插？

> [!IMPORTANT]
> **两个模块之间没有直接代码引用**。`CommercialApi` 不 import `EShangApiMain` 的任何内容，反之亦然。

它们是**完全独立的两套路由**，对应 C# 旧系统中两个独立的 Web API 项目：

| 维度 | CommercialApi | EShangApiMain |
|------|--------------|---------------|
| **C# 原项目** | CommercialApi.sln | EShangApiMain.sln |
| **路由前缀** | `/CommercialApi/*` | `/EShangApiMain/*` |
| **路由文件数** | 11 个 | 45+ 个 |
| **代码量** | ~750KB（revenue_router 独占 420KB） | ~300KB |
| **业务定位** | 经营看板/报表/分析（只读为主） | CRUD 数据管理（增删改查） |
| **接口风格** | 大量复杂聚合查询，多表关联 | 标准 CRUD，单表操作为主 |
| **services 使用** | ❌ 不使用（逻辑内嵌在 router） | ✅ 使用 services 层 |

### 2.2 关联点

虽然代码不直接引用，但两个模块在**业务数据层面有关联**：

```mermaid
graph LR
    subgraph CommercialApi
        REV["Revenue 接口<br/>读取营收汇总/分析"]
        BIG["BigData 接口<br/>读取车流数据"]
    end

    subgraph EShangApiMain
        BI_SP["ServerPartShop<br/>门店 CRUD"]
        BI_BP["BusinessProject<br/>经营项目 CRUD"]
        RV_CRUD["Revenue CRUD<br/>营收数据录入"]
        BD_CRUD["BigData CRUD<br/>车流数据录入"]
    end

    subgraph DB["同一个数据库"]
        T1["T_SERVERPARTSHOP"]
        T2["T_BUSINESSPROJECT"]
        T3["T_REVENUE"]
        T4["T_SECTIONFLOW"]
    end

    BI_SP -->|增删改| T1
    BI_BP -->|增删改| T2
    RV_CRUD -->|增删改| T3
    BD_CRUD -->|增删改| T4

    REV -->|只读查询| T1
    REV -->|只读查询| T2
    REV -->|只读查询| T3
    BIG -->|只读查询| T4

    style CommercialApi fill:#e1f5fe
    style EShangApiMain fill:#fff3e0
```

**总结**: EShangApiMain 负责**数据的写入和管理**，CommercialApi 负责**数据的汇总分析和展示**。两者通过共享的数据库表产生间接关联。

---

## 三、公用方法详解

### 3.1 公用层全景

```mermaid
graph TB
    subgraph CA["CommercialApi 路由"]
        CA1["revenue_router"]
        CA2["bigdata_router"]
        CA3["其他 8 个路由"]
    end

    subgraph EA["EShangApiMain 路由"]
        EA1["base_info/* (22个)"]
        EA2["contract/* (9个)"]
        EA3["finance/* (4个)"]
        EA4["其他路由"]
    end

    subgraph Core["core/ 核心工具层"]
        DB_H["database.py<br/>DatabaseHelper"]
        AES["aes_util.py<br/>AES 加解密"]
        DES["des_helper.py<br/>DES 加密"]
        FMT["format_utils.py<br/>格式化工具"]
        PROXY["old_api_proxy.py<br/>旧接口代理"]
    end

    subgraph Models["models/ 数据模型"]
        RESULT["base.py<br/>Result / JsonListData"]
        SEARCH["common_model.py<br/>SearchModel / NestingModel"]
    end

    subgraph Deps["routers/deps.py"]
        GET_DB["get_db()<br/>数据库连接注入"]
    end

    subgraph Services["services/ 业务逻辑层"]
        SV1["revenue/"]
        SV2["merchants/"]
        SV3["contract/"]
        SV4["...其他 13 个"]
    end

    CA1 & CA2 & CA3 --> DB_H
    CA1 --> DES
    CA1 & CA2 & CA3 --> AES
    CA3 --> PROXY

    EA1 & EA2 & EA3 & EA4 --> DB_H
    EA1 & EA2 & EA3 & EA4 --> RESULT
    EA1 & EA2 & EA3 & EA4 --> SEARCH
    EA1 & EA2 & EA3 & EA4 --> SV1 & SV2 & SV3 & SV4

    CA1 & CA2 & CA3 --> GET_DB
    EA1 & EA2 & EA3 & EA4 --> GET_DB

    SV1 & SV2 & SV3 & SV4 --> DB_H

    style Core fill:#e8f5e9
    style Models fill:#f3e5f5
    style Services fill:#fff3e0
```

### 3.2 各公用模块说明

| 公用模块 | 文件 | 功能 | CommercialApi 使用 | EShangApiMain 使用 |
|---------|------|------|-------------------|-------------------|
| **DatabaseHelper** | `core/database.py` | 达梦数据库连接池、查询封装、事务管理 | ✅ 所有路由 | ✅ 所有路由 |
| **AES 加解密** | `core/aes_util.py` | POST 请求体 AES 解密（`decrypt_post_data`） | ✅ 多个路由 | ❌ |
| **DES 加密** | `core/des_helper.py` | `MERCHANTS_ID` 等敏感 ID 加密（`des_encrypt_id`） | ✅ revenue_router | ❌ |
| **格式化工具** | `core/format_utils.py` | 日期、数值格式化 | ✅ | ✅ |
| **旧接口代理** | `core/old_api_proxy.py` | 未平移接口透传到 C# 旧服务 | ✅ budget_router | ❌ |
| **Result 模型** | `models/base.py` | 统一响应格式 `Result.success/fail`、分页 `JsonListData` | ✅ 所有路由 | ✅ 所有路由 |
| **SearchModel** | `models/common_model.py` | POST 搜索请求体模型（分页+排序+条件） | ❌ | ✅ 大部分路由 |
| **get_db()** | `routers/deps.py` | FastAPI 依赖注入，获取数据库连接 | ✅ 所有路由 | ✅ 所有路由 |
| **services 层** | `services/` | 业务逻辑封装（CRUD 操作） | ❌ 不使用 | ✅ 所有路由 |

### 3.3 两个模块的设计差异

```
CommercialApi（报表分析型）:
  路由文件 → 直接写 SQL + 业务逻辑 → core/database.py → DB
  特点: 大函数、复杂 SQL、多表关联、逻辑内嵌
  原因: C# 原实现也是 Controller 中直接写 SQL（AccountHelper 静态方法）

EShangApiMain（CRUD 管理型）:
  路由文件 → services/ 业务层 → core/database.py → DB
  特点: 标准 CRUD、单表操作、逻辑分层
  原因: 更早期平移，采用了分层架构
```

---

## 四、数据流向

```mermaid
sequenceDiagram
    participant FE as 前端
    participant MW as 中间件
    participant RT as 路由层
    participant SVC as Services
    participant DB as DatabaseHelper
    participant DM as 达梦数据库

    FE->>MW: HTTP 请求
    MW->>MW: 查询参数清洗
    MW->>RT: 分发到对应路由

    alt CommercialApi（报表型）
        RT->>DB: 直接构建 SQL 查询
        DB->>DM: 执行 SQL
        DM-->>DB: 返回 dict 列表
        DB-->>RT: 查询结果
        RT->>RT: 内存中处理数据<br/>(过滤/汇总/格式化)
        RT-->>FE: Result.success(data)
    end

    alt EShangApiMain（CRUD型）
        RT->>SVC: 调用 Service 方法
        SVC->>DB: 构建 SQL 查询
        DB->>DM: 执行 SQL
        DM-->>DB: 返回结果
        DB-->>SVC: 查询结果
        SVC-->>RT: 处理后数据
        RT-->>FE: Result.success(data)
    end

    alt 异常情况
        RT->>MW: 抛出异常
        MW-->>FE: Result.fail(msg)
    end
```

---

## 五、C# 原项目对照

```mermaid
graph LR
    subgraph CSharp["C# 旧系统（两个独立项目）"]
        CS_CA["CommercialApi.sln<br/>GeneralMethod/Revenue/<br/>AccountHelper.cs"]
        CS_EA["EShangApiMain.sln<br/>Controllers/<br/>各 Controller.cs"]
        CS_CORE["共享 DLL<br/>RealEstate.Core<br/>HWSB.dll / CMB.dll"]
    end

    subgraph Python["Python 新系统（合并为一个项目）"]
        PY_CA["routers/commercial_api/<br/>revenue_router.py 等"]
        PY_EA["routers/eshang_api_main/<br/>各路由文件"]
        PY_CORE["core/<br/>database.py / aes_util.py"]
        PY_SVC["services/<br/>业务逻辑层"]
    end

    CS_CA -->|平移| PY_CA
    CS_EA -->|平移| PY_EA
    CS_CORE -->|平移| PY_CORE
    CS_CORE -->|平移| PY_SVC

    style CSharp fill:#ffebee
    style Python fill:#e8f5e9
```

| C# 原始 | Python 对应 | 说明 |
|---------|------------|------|
| `AccountHelper.cs` 静态方法 | `revenue_router.py` 函数内嵌逻辑 | 1:1 平移，保持同样的 SQL 和处理逻辑 |
| `HWSB.SERVERPARTSHOP` ORM 类 | `services/base_info/` + 原生 SQL | ORM → 原生 SQL |
| `CMB.AUTOSTATISTICS.GetSubType()` | `revenue_router.py` 内嵌递归展开 | 公用方法变内联 |
| `RealEstate_System.ToDecrypt()` | `core/des_helper.py` | 加解密工具 |
| `Web.config` | `config.py` + `.env` | 配置管理 |
