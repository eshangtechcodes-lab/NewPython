# 中间件参数映射表

> **自动生成于**: 2026-03-13 | **文件**: `middleware/query_cleanup.py`

---

## 参数别名映射 (PARAM_ALIAS_MAP)

> [!IMPORTANT]
> 新增路由的参数名**必须**与此映射表同步！特别注意 `ID` vs `Id` 的大小写差异（参见知识手册坑#12）。

| 标准 key（lowercase） | 生成的别名列表 |
|----------------------|----------------|
| `serverpartid` | `ServerpartId`, `serverpartId`, `serverPartId`, `ServerpartID` |
| `provincecode` | `ProvinceCode`, `provinceCode` |
| `pageindex` | `PageIndex`, `pageIndex` |
| `pagesize` | `PageSize`, `pageSize` |

> 共 4 个参数别名映射。

## 布尔值转换规则

**True 值**: 未找到定义

**False 值**: 未找到定义

---

## 中间件处理流程

```
请求进入 → QueryParamCleanupMiddleware
  1. 遍历所有 query params
  2. 删除空值参数（空字符串/空白字符）
  3. 布尔值标准化（true/false/True/False → Python bool）
  4. 参数别名生成（根据 PARAM_ALIAS_MAP）
  5. 传递给下游路由
```
