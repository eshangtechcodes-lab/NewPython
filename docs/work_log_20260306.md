# 工作日志 2026-03-06

## 会话目标
继续迁移 `BaseInfoController.cs` 中的散装独立接口，严格按 SOP 逐个执行。

## 已完成接口

### 5. GetSPRegionShopTree ✅
- 路由: `BaseInfo/GetSPRegionShopTree` | GET
- 参数: `ProvinceCode`(int?), `ServerpartId`, `ServerpartCodes`(Query+Header), `ServerpartShopId`, `BusinessState`, `BusinessType`, `ShowWholePower`, `ShowState`, `ShowShortName`, `ShowInSale`, `SortStr`, `ShowRoyalty`, `ShowUnpaidExpense` (13个参数)
- 返回: `JsonMsg<JsonList<NestingModel<CommonTypeTreeModel>>>` — 区域→服务区→门店三级嵌套树
- 关键实现:
  - 三级树: 区域(type=0) → 服务区(type=1, NestingModel.node) → 门店(type=2, node.children)
  - `NestingModel<T>` = `{ node: T, children: List<NestingModel<T>> }`
  - JOIN `T_SERVERPART A, T_SERVERPARTSHOP B`
  - 省份编码映射: SQL 子查询 `PROVINCE_CODE = (SELECT FIELDENUM_ID...)`
  - ShowShortName 按简称分组模式
  - ShowState 追加经营状态标签
  - desc 显示门店数量
  - 无区域归属的服务区追加到顶层
- 基准数据: 7 个区域，143 个服务区，2876 个门店
- 对比结果: 全部通过

### 6. GetServerpartShopDDL ✅
- 路由: `BaseInfo/GetServerpartShopDDL` | GET
- 参数: `ProvinceCode`(int?), `ServerpartId`(str), `ShowWholePower`(bool) + Header: ServerpartCodes, ProvinceCode
- 返回: `JsonMsg<JsonList<CommonModel>>` — `{label=SHOPNAME, value=SERVERPARTSHOP_ID(str)}` 列表
- 关键实现:
  - 单表查询 `T_SERVERPARTSHOP WHERE ISVALID = 1`
  - 过滤优先级: ServerpartCodes > ServerpartId > ProvinceCode(EXISTS子查询)
  - 排序: SERVERPART_NAME, SHOPTRADE, SHOPSHORTNAME, SHOPREGION, SHOPCODE
  - value 字段为字符串类型
- 基准数据: 54 条(ServerpartId=416)
- 对比结果: 全部通过

### 7. GetServerpartShopTree ✅
- 路由: `BaseInfo/GetServerpartShopTree` | GET
- 参数: `ProvinceCode`(int?), `ServerpartId`(str), `ServerpartCodes`(str/Header), `ServerpartShopId`(str), `BusinessState`(str), `BusinessType`(str), `ShowWholePower`(bool), `ShowState`(bool)
- 返回: `JsonMsg<JsonList<NestingModel<CommonTypeTreeModel>>>` — 服务区→门店二级树（无区域层）
- 关键实现:
  - 与 GetSPRegionShopTree 类似但无区域分组层
  - 复用 `_bing_serverpart_shop_tree` 函数
  - JOIN `T_SERVERPART A, T_SERVERPARTSHOP B`
- 基准数据: 1 个服务区，54 个门店(ServerpartId=416)
- 对比结果: 全部通过

### 8. GetNestingOwnerUnitList ✅
- 路由: `BaseInfo/GetNestingOwnerUnitList` | GET
- 参数: `OwnerUnitPID`(int?,-1), `ShowStatus`(bool), `ProvinceCode`(int?), `OwnerUnitNature`(str), `OwnerUnitName`(str)
- 返回: `JsonMsg<JsonList<NestingModel<OWNERUNITModel>>>` — 递归嵌套树(18字段)
- 关键实现:
  - 查 `T_OWNERUNIT` 单表，按 OWNERUNIT_PID 递归构建父子树
  - `_bind_ownerunit_data` 递归函数 + `_ownerunit_row_to_model` 字段映射
  - 支持模糊查询过滤 `OwnerUnitName`
- 基准数据: 21 顶层节点，592 递归节点总数
- 对比结果: 全部通过

### 9. BindingOwnerUnitDDL ✅
- 路由: `BaseInfo/BindingOwnerUnitDDL` | GET
- 参数: `ProvinceCode`(int?), `DataType`(int,0) + Header: UserPattern, ProvinceCode
- 返回: `JsonMsg<JsonList<CommonTypeModel>>` — {label, value, ico}
- 关键实现:
  - WHERE PID=-1 AND NATURE=1000
  - DataType=0: value=PROVINCE_BUSINESSCODE; DataType=1: value=OWNERUNIT_ID
  - ico = CoopMerchantUrl + OWNERUNIT_ICO
- 基准数据: 19 条
- 对比结果: 全部通过

### 10. BindingOwnerUnitTree ✅
- 路由: `BaseInfo/BindingOwnerUnitTree` | GET
- 参数: `DataType`(int,0), `OwnerUnitPid`(int?,-1)
- 返回: `JsonMsg<JsonList<NestingModel<CommonTypeModel>>>` — 递归嵌套树
- 关键实现:
  - Controller 固定传 OwnerUnitNature=1000
  - 递归 PID 用 value(而非 OWNERUNIT_ID)，与原 C# 一致
  - DataType=0: value=PROVINCE_BUSINESSCODE; DataType=1: value=OWNERUNIT_ID
  - ico = CoopMerchantUrl + OWNERUNIT_ICO
- 基准数据: 19 个节点
- 对比结果: 全部通过

### 11. BindingMerchantTree ✅
- 路由: `BaseInfo/BindingMerchantTree` | GET
- 参数: `MerchantPid`(int?,-1), `ProvinceCode`(int?)
- 返回: `JsonMsg<JsonList<NestingModel<CommonTypeModel>>>` — 递归嵌套树
- 关键实现:
  - 复用 `binding_ownerunit_tree`，参数 Nature=2000, DataType=1, ProvinceCode
  - 无需新 Service 函数
- 基准数据: 1 顶层节点，549 递归节点
- 对比结果: 全部通过

## 修改的文件
- `services/base_info/base_info_misc_service.py` — 添加 6 个接口的 Service 函数
- `routers/eshang_api_main/base_info/base_info_misc_router.py` — 修正 6 个接口的返回结构

---

## 接口 8: GetBusinessBrandList（序号 12）

- 路由: `BaseInfo/GetBusinessBrandList`
- HTTP 方法: GET
- 功能: 查询门店经营品牌列表
- 参数: `ServerpartShopIds`(string), `ShowWholePower`(bool)
- 原 C# 逻辑:
  - 查 `T_SERVERPARTSHOP` 按门店 ID 筛选有 BUSINESS_BRAND 的记录
  - 去重品牌 ID 后逐个查 `T_BRAND` 品牌全量字段 (BindObjectToModel)
  - 拼接 `BRAND_INTRO` URL 前缀 (CoopMerchantUrls)
  - 附带 `ServerpartList` (服务区名称去掉"服务区"后缀)
  - 按 `BRAND_NAME` 排序
- 关键实现:
  - Service: `get_business_brand_list()` — 完整复现上述逻辑
  - Router: 添加 `ShowWholePower` 参数和 `ServerpartShopIds` Header 读取
  - `_format_datetime()` 辅助函数处理日期格式
  - `BRAND_INDUSTRY` str→int 类型转换 (P1 已知问题)
- 基准数据: 4 条品牌记录，28 个字段含 ServerpartList 嵌套
- 对比结果:
  - ✅ Result_Code=100, TotalCount=4
  - ✅ 字段列表完整, 数据类型正确
  - ✅ ServerpartList 嵌套结构正确
  - ⚠️ OPERATE_DATE 仅日期（达梦 DATE 类型不含时间，已知差异）
  - ⚠️ MANAGE_TYPE/COMMISSION_RATIO 值不同（数据库数据差异，非代码问题）

## 修改的文件（续）
- `services/base_info/base_info_misc_service.py` — 添加 `get_business_brand_list` + `_format_datetime`
- `routers/eshang_api_main/base_info/base_info_misc_router.py` — 修正 GetBusinessBrandList 返回结构

---

## 接口 9: ModifyShopState（序号 13）

- 路由: `BaseInfo/ModifyShopState`
- HTTP 方法: POST
- 功能: 批量变更门店经营状态
- 入参: `List<ShopModifyModel>` (13字段: SERVERPARTSHOP_ID, BUSINESS_STATE, BUSINESS_DATE, BUSINESS_ENDDATE, STAFF_ID, STAFF_NAME, OPERATE_DATE 等)
- 原 C# 逻辑:
  - 遍历列表→按 SERVERPARTSHOP_ID 查 T_SERVERPARTSHOP
  - 查 T_RTSERVERPARTSHOP (运营记录), 根据状态决定 Insert/Update
  - 状态 1000/1010: 更新 BUSINESS_DATE
  - 其他状态: 更新 BUSINESS_ENDDATE
  - SetShopBusinessState 更新 T_SERVERPARTSHOP.BUSINESS_STATE
- 关键实现:
  - Service: `modify_shop_state()` — 完整复现逻辑链
  - Router: Body 参数接收, 返回结构含 Result_Data: null
  - 省略 OperatePropertyShopStatus (资产同步) 和 RecordShopCount (辅助)
- 基准数据:
  - 空列表: Result_Code=200, "变更失败，数据更新失败！"
  - 有数据(门店3128, BUSINESS_STATE=1000): Result_Code=100, "变更成功"
- 对比结果:
  - ✅ 有数据请求: Result_Code=100, 变更成功
  - ✅ 空列表请求: Result_Code=200, 变更失败
  - ✅ Result_Data=null
  - ⚠️ 修复: SEQ_RTSERVERPARTSHOP 序列缺失, 改用 MAX+1 子查询

## 修改的文件（续）
- `services/base_info/base_info_misc_service.py` — 添加 `modify_shop_state`
- `routers/eshang_api_main/base_info/base_info_misc_router.py` — 修正 ModifyShopState Body参数 + 返回结构

## 待继续
- 下一个接口: `GetShopReceivables` (L3674) — GET 接口
- 剩余: 8 个散装接口
