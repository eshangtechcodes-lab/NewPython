# 2026-03-10 工作日志

## 今日完成

### 1. Revenue/GetServerpartINCAnalysis 接口核心修复与对齐 ✅
修复了该接口在“增幅分析”场景下与旧接口的 3 处核心口径差异：
- **节日名称映射修复**：补齐了 `HolidayType` 为 3/4/5/7/8 时对应的节日名称后缀（如“中秋节”），解决了因查不到 `T_HOLIDAY.HOLIDAY_DESC` 而导致返回 `101` 的问题。
- **businessRegion=1 过滤口径修正**：修正了经营区域过滤逻辑，将 `NOT IN (72)`（错误排除了片区）改为 `NOT IN (89)`（排除城市店及商城），确保多服务区查询时服务区列表与旧接口 100% 对齐。
- **NULL/0 数值口径对齐**：重构 `safe_or_none` 函数，在服务区记录存在但业务字段 SUM 为 NULL（SQL 行为）时强制返回 `0.0`，对齐了旧接口 C# `Convert.ToDecimal(DBNull) = 0` 的行为。

### 2. 多服务区样本精确验收 ✅
使用含 133 个服务区的全量固定样本进行了精确对比验证：
- **Result_Code / TotalCount / List 长度**：完全一致。
- **数据对齐**：RevenueINC、AccountINC、BayonetINC、SectionFlowINC 下的所有数值与子字段完全一致（含 0 值与 null 处理）。
- **OtherData**：节日日期描述文本完全对齐。

### 3. 全局风险自检与加固 (进行中)
- 扫描并排查了 `revenue_router.py` 中其他 11 个接口的节日名称对照逻辑，确认安全。
- 扫描了文件中所有 `ORDER BY` 排序逻辑，识别出多个接口仍仅使用 `SERVERPART_ID` 排序，存在排序口径风险，已记录风险位置清单以便后续调整为 `INDEX` 复合排序。

## 当前测试状态
- **GetServerpartINCAnalysis PASS: 133/133 (100.0%)**
- **差异变化明细**
| 接口 | 修改前一致率 | 修改后一致率 | 变化说明 |
|---|---|---|---|
| GetServerpartINCAnalysis | 0% (报错/ID错位) | 100.0% | 已完全闭环 |

## 关键发现
- **片区索引的重要性**：仅凭 ID 排序无法对齐旧接口，前端报表对服务区 `SPREGIONTYPE_INDEX` 和 `SERVERPART_INDEX` 有强依赖。
- **DBNull 的坑**：新架构在处理 SUM(NULL) 时需显式补齐为 `0.0`，否则会导致数值差异。

## Git 提交记录
1. `a74504b` 完成: GetShopSABFIList 深度对齐 (一致率 89.2%)，修复聚合行业/品牌、结算映射及图片路径前缀
2. `f7f48ad` 修复: GetShopSABFIList QOQ改用BW固化字段+过滤空门店+Brand_ICO精确对齐 (81门店77一致95.1%)
3. `7e669d3` 修复: GetShopSABFIList QOQ/Brand_ICO/BusinessTrade逻辑精确对齐 (84门店77一致92%)
4. `1463609` 优化: GetShopSABFIList QOQ null逻辑精确对齐 (84门店77一致 91.7%)
5. `9ea0c63` 完成: GetShopSABFIList 全字段对齐 (13/13门店零差异)
6. `eec7e94` fix: 统一所有路由文件的 ServerpartId 多ID处理，消除SQL注入风险
7. `b22654c` docs: 更新工作日志 - 多服务区兼容6/6全部通过

## 明日计划
1. 继续对 `revenue_router.py` 剩余接口执行排序口径加固。
2. 开始迁移下一模块接口并同步进行基线缓存对比。
