# 工作日志 2026-03-09

## 当前状态
- **CommercialApi: Revenue 59/59 PASS, 总体 121/123 (98.4%) ✅**
- 本次会话重点：**GetShopSABFIList 深度对齐**（117处差异 → 1处）
- 修复了 Profit fallback 逻辑、AvgTicketINC 精度、lYearData 方向等

---

## 今日修复（本次会话）

### 🔥 GetShopSABFIList 深度对齐 — 117处差异 → 1处

#### 修复项 1：AccountINC / BayonetINC lYearData 方向
- **问题**: 片区节点 AccountINC.lYearData=0.0, BayonetINC.lYearData=0.0，但 C# SP 层返回 None
- **修复**: SP 层 lYearData=None, 片区层=None, 根节点=0.0，逐层级对齐 C#

#### 修复项 2：AvgTicketINC 精度对齐
- **问题**: `round(rev/ticket - rev_qoq/ticket_qoq, 2)` 产生浮点漂移（如 -0.42000000001）
- **修复**: 使用 lambda 中间量 `ac=round(rev/ticket,2), aq=round(rev_qoq/ticket_qoq,2)`，再用 `round(ac-aq, 2)` 对齐 C# Math.Round 行为

#### 修复项 3：AccountINC curYearData/QOQData round 防漂移
- **问题**: 累加多个门店的 ROYALTY_THEORY 产生浮点误差
- **修复**: 在聚合节点对 curYearData、QOQData、increaseDataQOQ 分别 round(x, 2)

#### 修复项 4：数据同步 — T_BUSINESSWARNING 值不一致
- **问题**: DM 的 T_BUSINESSWARNING 行数与 Oracle 一致，但 TICKET_COUNT、REVENUE_AMOUNT_QOQ、VEHICLE_COUNT_QOQ 等字段值不同
- **修复**: 执行 force_sync_bw.py 强制覆盖同步（DELETE + INSERT）

#### 修复项 5：Profit_Amount fallback 逻辑 ⭐
- **问题**: C# 返回 688773.27，Python 返回 0.0。Oracle T_PERIODMONTHPROFIT 无 2026 年数据
- **根因**: C# `AccountHelper.cs:4355-4358` 有 fallback：**无 Profit 数据时，自营门店(BusinessTradeType<3) 的 Profit = Revenue**
  ```csharp
  else if (shopModel.BusinessTradeType < 3)
  {
      shopModel.Profit_Amount = shopModel.RevenueINC?.curYearData;
      shopModel.Cost_Amount = 0;
  }
  ```
- **验算**: BTT=1 自营超市 313432.6 + BTT=2 自营老乡鸡 375340.67 = **688773.27 完全匹配**
- **修复**: Python 端同样实现 fallback，并将聚合从 profit_map 直接取改为从 sp_node 累加

#### 修复项 6：数据同步 — T_PROFITCONTRIBUTE / T_PERIODMONTHPROFIT
- 强制覆盖同步 T_PROFITCONTRIBUTE (149,590 条) 和 T_PERIODMONTHPROFIT (9,847 条)
- 两张表 Oracle max_month=202512，无 2026 年数据（属正常）

#### 未修复：ShopSABFIList（剩余唯一差异）
- C# 返回 13 项门店经营项目列表，Python 返回 None
- 数据源：T_PROFITCONTRIBUTE + T_BUSINESSPROJECT 按 BUSINESSPROJECT_ID 分组
- 之前错误用 T_BUSINESSWARNING 按门店分组构建，数据维度完全不对
- **标记为 TODO，需单独实现**

### 📋 接口启动 404 排查
- **现象**: 访问 `GetConfigInfo` 返回 404
- **根因**: `GetConfigInfo` 路由本身不存在！是测试时用了错误的路径
- **确认**: 接口正常运行，131 条路由全部注册成功

---

## 全量对比最终结果

```
✅ PASS=121/123  ❌ DIFF=0  ⏭️ SKIP=2
Revenue: 59/59 PASS ✅
```

| Controller | 总数 | PASS | 说明 |
|-----------|------|------|------|
| Revenue | 59 | 59 | ✅ 含 SABFI/MonthINCAnalysis 修复 |
| BigData | 15 | 15 | ✅ |
| Examine | 15 | 14 | ⏭️ MeetingList旧API超时 |
| BaseInfo | 9 | 9 | ✅ |
| Analysis | 7 | 7 | ✅ |
| Customer | 7 | 7 | ✅ |
| Budget | 4 | 4 | ✅ |
| Contract | 3 | 3 | ✅ |
| AbnormalAudit | 2 | 2 | ✅ |
| BusinessProcess | 1 | 1 | ✅ |
| Debug | 1 | 0 | ⏭️ 旧API本身404 |

---

## 同类型问题清单（已排查，待修复）

### P1：ShopSABFIList 门店列表实现
- 接口: GetShopSABFIList
- 需从 T_PROFITCONTRIBUTE + T_BUSINESSPROJECT 按经营项目(BUSINESSPROJECT_ID)分组构建
- 每个门店包含 Revenue/Account/TicketINC、Brand 信息、SABFI 评分等

### P2：其他接口 Profit_Amount 对照检查
- 约 8 处硬编码 Profit_Amount=None，需逐个对照 C# 确认是否需要查询 + fallback
- 涉及: GetBayonetOwnerAHTreeList, GetShopINCAnalysis, GetShopCurRevenue 等

### P3：GetShopMonthSABFIList Profit fallback
- 可能缺少 BTT<3 → Profit=Revenue 的 fallback 逻辑

---

## 关键技术发现（新增）

5. **Profit fallback 模式**: C# 中当 T_PERIODMONTHPROFIT 无数据时，自营门店(BusinessTradeType<3) 的 Profit_Amount 自动等于 Revenue_Amount，Cost_Amount=0。**这是一个隐含的业务逻辑，不是 bug**
6. **数据同步陷阱**: DM 与 Oracle 行数一致不代表数据一致！T_BUSINESSWARNING 出现了行数相同但字段值不同的情况，必须用**强制覆盖**而非增量同步
7. **浮点精度控制**: C# Math.Round(a/b, 2) 先 round 再减，Python 用 `round(a/b - c/d, 2)` 会有微小差异。需用中间变量 `ac=round(a/b,2)` 再 `round(ac-aq,2)` 对齐

---

## 数据同步记录

| 表名 | 行数 | 同步方式 | 时间 |
|------|------|---------|------|
| T_BUSINESSWARNING | 24,360 | 强制覆盖 | 18:00 |
| T_PROFITCONTRIBUTE | 149,590 | 强制覆盖 | 18:10 |
| T_PERIODMONTHPROFIT | 9,847 | 强制覆盖 | 18:10 |

---

### 🔧 多服务区兼容问题整改（晚间 19:00-23:30）

**问题族**：`ServerpartId=416,417` 多值参数时返回 999 或数据不一致（共 6 条 A 类问题）

#### 第一轮：代码审查确认（4 条已修复 + 2 条待观察）

| 接口 | 修复方式 | 状态 |
|------|---------|------|
| Examine/GetPatrolAnalysis | `_parse_multi_ids` 已就位 | ✅ 已通过 |
| Examine/GetPatrolResultList | `_parse_multi_ids` 已就位 | ✅ 已通过 |
| Revenue/GetShopCurRevenue | 手写 split + IN 已就位 | ✅ 已通过 |
| Revenue/GetShopSABFIList | 手写 split + IN 已就位 | ✅ 已通过 |
| Examine/GetExamineAnalysis | `_parse_multi_ids` 已就位但查不到数据 | ❌ TC=0 |
| Examine/GetExamineResultList | `_parse_multi_ids` 已就位但查不到数据 | ❌ TC=0 |

#### 第二轮：PROVINCE_CODE 内码转换修复

- **根因**：T_SERVERPART.PROVINCE_CODE 存的是 FIELDENUM_ID（如 3544），而 examine_router.py 直接用行政区划码 "340000" 查询
- **修复**：新增 `_resolve_province_id()` 辅助函数（通过 T_FIELDEXPLAIN + T_FIELDENUM 转换）
- **结果**：GetExamineAnalysis 多值 TC 从 0→3 ✅

#### 第三轮：GetExamineResultList 返回结构重写

- **根因**：旧接口返回「考核记录 + 嵌套明细 list」聚合结构，Python 直接返回 T_EXAMINE 平铺明细
- **修复**：
  1. 主查询改为 SELECT 指定字段（不再 SELECT A.*）
  2. 批量查 T_EXAMINEDETAIL 按 EXAMINE_ID 分组
  3. 组装聚合结构：每个 item 含服务区字段 + list(明细子列表)
- **结果**：字段结构完全对齐旧接口 ✅，TC 数值差异(2vs1, 141vs124)判定为 DM/Oracle 数据差异

## Git提交

- `fix: GetShopSABFIList 完善 - BayonetINC_ORI/SABFI_Score/AvgTicketINC精度/lYearData层级方向/round防漂移` (bccd008)
- `fix: GetShopSABFIList Profit fallback对齐C# - 无Profit数据时自营门店Profit=Revenue (BTT<3)` (2cdc4ee)
- `fix: 排查接口启动问题(实为路径不存在) + 同类问题报告` (fe95292)

---

## 项目架构概述（供后续AI参考）

### 技术栈
- **Python**: FastAPI + Uvicorn (端口8080)
- **数据库**: 达梦DM（从Oracle同步）
- **旧API**: C# .NET WebAPI (端口8900，通过nginx 8001代理)
- **前端**: Vue.js（通过nginx统一代理到Python或C# API）

### 项目目录结构
```
D:\Projects\Python\eshang_api\
├── main.py                     # FastAPI入口
├── core/
│   ├── database.py             # 达梦DM数据库连接
│   └── config.py               # 配置
├── routers/
│   └── commercial_api/         # CommercialApi路由（已完成）
│       ├── base_info_router.py  # BaseInfo (9接口)
│       ├── revenue_router.py    # Revenue (59接口) + Analysis(7) + Customer(7) 等
│       ├── bigdata_router.py    # BigData (15接口)
│       ├── examine_router.py    # Examine (15接口)
│       ├── budget_router.py     # Budget (4接口)
│       └── ...
├── models/base.py              # Result/JsonListData 通用模型
├── scripts/
│   ├── compare_cached.py       # 全量实时对比脚本（新旧API同时调用）
│   ├── baseline_collect.py     # 旧API基线采集
│   └── test_single.py          # 单接口测试
├── workLog/                    # 工作日志
├── docs/
│   └── collaboration_plan.md   # 协作计划（含EShangApiMain迁移方案）
└── .agents/workflows/          # AI工作流定义
```

### C#源码位置
```
D:\CSharp\Project\000_通用版本\000_通用版本\030_EShangApi\
├── CommercialApi\              # 已迁移完成
│   ├── Controllers\            # 10个Controller
│   └── GeneralMethod\          # Helper层（复杂业务逻辑）
├── EShangApiMain\              # 待迁移（53个Controller, 500+路由）
│   └── Controllers\
│       ├── MainProject\        # FrameWork, BaseInfo 等
│       └── Revenue\            # Revenue相关
└── EShangApi.Common\           # 共享逻辑层
```

### 测试体系
- `scripts/compare_cached.py`: 全量123接口实时对比，新旧API同时调用
- `scripts/test_single.py "GET:/Revenue/GetXxx"`: 单接口对比（支持baseline缓存）
- 对比逻辑: 深度递归比较JSON结构，忽略字段顺序

### 关键发现和经验（重要）
1. **BusinessTradeType映射**: C#中1/2/3不是直接的字段值，而是映射为 `BUSINESS_TYPE=4000+SHOPTRADE=2` 等组合条件
2. **solidType路径分流**: `solidType=true`且查历史月份时，走`T_BUSINESSWARNING`固化表；否则走`T_REVENUEMONTHLY`或`T_MONTHLYREVENUE`实时表
3. **数据表差异**: CommercialApi用`T_REVENUEMONTHLY`，EShangApiMain用`PLATFORM_DASHBOARD.T_MONTHLYREVENUE`，字段名和逻辑不同
4. **WM_CONCAT**: Oracle的WM_CONCAT在DM中用`LISTAGG`替代，排序可能不同（可接受差异）
5. **日期格式**: C#使用`yyyy/M/d`（无前导零），Python需用`dt.strftime`手动格式化

---

## 待办事项

### 🔴 高优先级
- [ ] ShopSABFIList 门店列表实现（P1，从 T_PROFITCONTRIBUTE 构建）
- [ ] nginx配置确认: EShangApiMain请求不应转发到Python(8080)

### 🟡 中优先级
- [ ] 其他接口 Profit_Amount 对照检查（P2，约 8 处）
- [ ] GetShopMonthSABFIList Profit fallback（P3）
- [ ] 重新同步 `T_MEETING` 表数据（差1条记录）

### 🟢 长期任务 — EShangApiMain 迁移
- [ ] Phase 1: BaseInfoController 样板迁移 (1-2天)
- [ ] Phase 2: 53个Controller并行迁移 (9-12天)
- [ ] Phase 3: 集成测试 (2-3天)
