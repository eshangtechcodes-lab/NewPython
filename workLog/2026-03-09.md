# 2026-03-09 工作日志

## 📊 API接口迁移对比修复 (续)

### 起始状态
- PASS: 49/74 (66.2%)
- 主要问题: HolidayAnalysis/Batch逻辑缺陷, ProvinceAvgBayonetAnalysis逻辑错误

### 修复内容

#### 1. GetHolidayAnalysis: 69处差异→PASS ✅
- 修复`_sum_compute`小数格式化：确保小数至少2位（`252451.4`→`252451.40`），匹配C# `Decimal.ToString()`
- SQL加`ROUND(SUM(...),2)`强制2位小数

#### 2. GetHolidayAnalysisBatch: 30处差异→PASS ✅
- null化Batch中C#不返回的7个字段（`curYearSRRevenue`等）

#### 3. GetCompanyRevenueReport: 13处差异→3处 📉
- **关键bug**: `T_AUTOSTATISTICS.PROVINCE_CODE`用的是FIELDENUM_ID(3544)而非实际省份代码(340000)，导致查询返回0行
- 修复`PROVINCE_CODE = {ProvinceCode}`
- 叶子节点children改为None（C# null）
- BusinessTrade_Id改为float类型
- ServerpartShop_Id保留原始遍历顺序（dict.fromkeys去重）
- 剩余3处差异是SQL GROUP BY结果集排序不同，Oracle vs 达梦差异

#### 4. GetSummaryRevenue: 结构性修复（部分）
- 重写查询逻辑：直接查询T_REVENUEDAILY日期范围
- 修复Statistics_StartDate被忽略的问题
- RevenuePushModel从None变为有数据

#### 5. baseline重建
- 重新请求旧API获取最新数据，更新105个接口
- 消除因数据漂移导致的误判DIFF

### 结束状态
- **实时对比 PASS: 52/74 (70.3%)**
- 新增PASS: HolidayAnalysis + HolidayAnalysisBatch + ProvinceAvgBayonetAnalysis
- CompanyRevenueReport: 13→3处差异
- 本次会话3个commit

### 剩余DIFF分类 (22个)
- 🟢 数据变化（无法修复）：DateAnalysis(178), MonthAnalysis(8), TransactionConvert(10), BusinessBrandLevel(14), BusinessTradeLevel(15), BusinessTradeRevenue(18), BayonetOAList(4), BayonetProvinceOAList(4), UnUpLoadShops(50), MonthINCAnalysisSummary(1, 数据微变)
- 🔴 代码逻辑：RevenueCompare(28, 工作日/周末/节假日平均), MonthINCAnalysis(41, QOQ/YOY), SummaryRevenue(13), SummaryRevenueMonth(20)
- 🟡 其他：CompanyRevenueReport(4, SQL排序), BayonetOwnerAHTreeList(22), ShopSABFIList(48), RevenueReportDetil(73), HolidaySPRAnalysis(86, 数据变化大), ProvinceVehicleTreeList(88, 数据变化), SalableCommodity(4), BudgetExpenseList(3, POST策略), RevenueTrendChart(POST加密)
