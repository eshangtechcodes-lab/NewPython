# 2026-03-11 工作日志 — Revenue API 对齐与修复

## 📋 完成事项

### 1. ✅ `GetSPRevenueRank` 接口完全对齐

**问题**：冒烟回归中新接口 `999`，旧接口 `100`。

**修复**：
- `OtherData: Optional[dict]` → `Optional[Any]`（`models/base.py`）
- `JsonListData.create(other_data=...)` → `JsonListData(OtherData=...)`
- 添加 PROVINCE_CODE 过滤（通过 T_FIELDENUM 转换）
- 添加 `STATISTICS_TYPE=1000`, `STATISTIC_TYPE=1000` 双条件
- 添加 `Revenue_Include=1` EXISTS 子查询
- 添加测试服务区排除（`SERVERPART_CODE NOT IN`）
- C# 当日分支不查 RevenueYOY → 设 None
- YearAccountRoyalty 默认 0.0

**验证**（`pushProvinceCode=340000, Statistics_Date=2026-02-01, Revenue_Include=1`）：
- TotalCount: 136 = 136 ✅
- OtherData: 3824703.82 = 3824703.82 ✅
- 第1条全30字段一致 ✅

---

### 2. 🔍 `GetTransactionTimeAnalysis` 排查

**问题**：旧接口 `100`，新接口 `101`。

**根因**：`T_YSSELLMASTERMONTH` 表未同步到达梦（数据同步遗漏，非代码问题）。
- SQL 逻辑与 C# 完全一致
- 表内完全无数据（所有月份、所有参数返回 0 行）
- 该表未在 `sync_oracle_to_dm.py` 同步列表中

**额外修复**：补充 Province_Code else 分支过滤。

**阻塞**：本机无 64 位 Oracle Client，无法同步数据。需在有 Oracle Client 的机器上执行。

---

## 📁 修改文件

| 文件 | 变更 |
|------|------|
| `routers/commercial_api/revenue_router.py` | 重写 GetSPRevenueRank；GetTransactionTimeAnalysis 补 Province_Code 过滤 |
| `models/base.py` | `OtherData: Optional[dict]` → `Optional[Any]` |

## ⏳ 待处理

- [ ] 同步 `T_YSSELLMASTERMONTH` 表到达梦
- [ ] 同步后重新验证 GetTransactionTimeAnalysis
- [ ] 继续 123 接口全量冒烟回归
