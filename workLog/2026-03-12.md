# 2026-03-12 工作日志 — GetMonthINCAnalysis 报文字段精确收尾

## 📋 完成事项

### 1. ✅ `GetMonthINCAnalysis` 报文字段精确对齐（本轮核心）

**目标**：在前一轮三大逻辑修复（跨月查询、showFilter过滤、calcBayonet聚合）基础上，做报文级别的字段精确收尾，使旧新 JSON 整包完全一致。

**修改文件**：`routers/commercial_api/revenue_router.py`（+461/-132）

#### 修复项 1：`increaseData` 默认回填去掉
- **问题**：`_make_inc` 在 `curYearData=0` 时仍计算 `0-0=0.0` 填入 `increaseData`，旧接口为 `null`
- **修复**：加 `cur_v != 0` 判定，对齐 C# 的 `TryParseToDecimal() != 0` 逻辑
- **效果**：S2(showFilter) 从 7 diffs → **0 diffs PASS**

#### 修复项 2：`CompactDate` 多余字段删除
- **问题**：ShopINCList 中多出 `"CompactDate": null`，旧接口无此字段
- **修复**：删除 `CompactDate` 行
- **效果**：S3 减少 17 个 diffs

#### 修复项 3：`BayonetINC` 缺失→显式 null 对象
- **问题**：根节点/片区 `BayonetINC` 输出 JSON `null`，旧接口为全 null 对象 `{"curYearData":null,...}`
- **修复**：
  - `_make_node` 默认参数改为 `bayonet=_BAYONET_NULL`（`_make_inc(None, None)`）
  - 固化路径片区/根级：默认全 null 对象
  - 非固化路径服务区级：显式传 `bayonet=None`（旧接口该级确实返回 null）

#### 附带修复
- 非固化路径片区/根 `lYearData`/`QOQData`：`None` → `0.0`，对齐 C# LINQ Sum 空序列=0
- 非固化路径增加 `avg_ticket` 计算
- 非固化路径服务区级增加 `bayonet=None`

### 2. ✅ 补提交遗留文件
- `models/base.py`：`OtherData: Optional[dict]` → `Optional[Any]`（修复 GetSPRevenueRank 999 问题）
- `workLog/2026-03-11.md`：3-11 工作日志
- `workLog/p0_matrix_v2.json`：P0 接口参数矩阵测试用例
- `workLog/p0_matrix_results.json`：矩阵测试结果

## 📊 验证结果（3 样本 deep diff）

| 样本 | 参数特征 | 初始 diffs | 最终 diffs | 状态 |
|------|---------|-----------|-----------|------|
| S1 跨月 | 202601~202602 | 22 | **1** | ShopINCList 非固化缺门店明细 |
| S2 showFilter | showRevenue=1,showBayonet=1 | 7 | **0** | ✅ PASS |
| S3 calcQOQ | calcQOQ=true,calcYOY=true | 281 | **264** | 全是门店排序差异 |

**S3 剩余说明**：264 个 diff 全部在 `ShopINCList[8]~[14]`，前 0-7 完全一致。C# 无显式门店排序，差异来自 Oracle vs Dameng 数据库默认行为，不影响数据正确性。

## 🔀 Git 提交

| Commit | 说明 |
|--------|------|
| `c74a4c0` | fix: GetMonthINCAnalysis 报文字段精确对齐 |
| `cb9c171` | chore: 提交base.py修复 + 工作日志及矩阵测试结果 |

## 📌 遗留项

- [ ] S3 门店排序差异（Oracle vs Dameng 默认行为，非代码逻辑问题）
- [ ] S1 非固化路径 ShopINCList 缺门店明细数据
